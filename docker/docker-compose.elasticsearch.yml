services:
  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.15.0
    container_name: db-mcp-elasticsearch
    network_mode: host
    environment:
      discovery.type: single-node
      xpack.security.enabled: 'false'
      http.port: '9201'
      transport.port: '9301'
      network.host: 127.0.0.1
      ES_JAVA_OPTS: -Xms512m -Xmx512m
    volumes:
      - elasticsearch-data:/usr/share/elasticsearch/data
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://127.0.0.1:9201/_cluster/health >/dev/null"]
      interval: 15s
      timeout: 10s
      retries: 20
      start_period: 40s

  elasticsearch-seed:
    image: python:3.10-slim
    container_name: db-mcp-elasticsearch-seed
    restart: "no"
    network_mode: host
    depends_on:
      elasticsearch:
        condition: service_healthy
    working_dir: /workspace/db-mcp-server
    volumes:
      - ..:/workspace/db-mcp-server
    environment:
      DB_MCP_TEST_ELASTICSEARCH_URL: http://127.0.0.1:9201
      DB_MCP_TEST_OPENSEARCH_PREFIX: dbmcp_ecommerce
    command: >-
      bash -lc "pip install --no-cache-dir requests >/tmp/pip.log 2>&1 &&
      python -m tests.fixtures.elasticsearch_seed"

volumes:
  elasticsearch-data:
