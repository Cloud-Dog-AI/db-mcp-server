{
  "openapi": "3.0.3",
  "info": {
    "title": "db-mcp-server",
    "version": "0.0.0",
    "description": "Generated stub — build pipeline replaces this with the live spec via cloud_dog_api_kit.openapi.generate(). See docs/API-REFERENCE.md and docs/BUILD.md."
  },
  "paths": {
    "/health": {
      "get": {
        "summary": "Liveness probe",
        "responses": { "200": { "description": "OK" } }
      }
    }
  }
}
